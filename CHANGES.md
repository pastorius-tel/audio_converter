# 修正内容一覧 (v0.2.0 → v0.3.0) — GitHub 公開に向けた最終確認

GitHub への公開に向けて、セキュリティ・UI・バグ・文字列パース処理の
確認を行った。今回は初めて実際の Rust ツールチェーン(rustc 1.95)で
`cargo build --release` / `cargo test` / `cargo clippy` / `cargo fmt --check`
をフルに実行して検証できたため(これまでのサンドボックスは rustc が
古く GUI 依存クレートを実際にビルドできなかった)、以前は静的な
コードレビューでしか確認できなかった問題も実行時レベルで洗い出せた。

| # | 症状 | 原因 | 修正内容 |
|---|---|---|---|
| 1 | Windows 以外の環境(Linux上のCIや `cargo check` など)で `cargo build` すると、`build.rs` が `winresource` を解決できずコンパイルエラーになる | `[target.'cfg(windows)'.build-dependencies]` の `cfg(windows)` は「ビルド対象OS」ではなく「ビルドスクリプトを実際にコンパイル・実行するホストOS」に対して評価される。そのため Windows 以外のホストでは `winresource` が依存関係として解決されず、`build.rs` 内の参照がコンパイルエラーになっていた(実機での `cargo build --release` 成功はホストがWindowsだったため気づかなかった) | `winresource` を無条件の `[build-dependencies]` に変更(実行対象の絞り込みは既存の `CARGO_CFG_TARGET_OS` 実行時チェックに任せる)。Linux ホストで `cargo build --release` が通ることを確認済み |
| 2 | ffmpeg パス欄を空にすると「● PATH から解決します」という(誤解を招く)表示になる | `is_bare_command("")` が true を返すため、空文字も「裸のコマンド名」として扱われていた | 空欄(空白のみ含む)は専用の「● ffmpeg のパスが未設定です」表示にし、この状態では「変換実行」ボタンも無効化するよう修正 |
| 3 | 「既定」ボタン(自動検出に戻す)を押しても、保存済みの有効なパスがある限り何も変わらない | 「既定」ボタンが `initial_ffmpeg_path()`(=起動時ロジック。保存済み設定を最優先する)をそのまま呼んでいたため、保存済みの値をそのまま読み戻すだけになっていた | 保存済み設定を一切参照せず自動検出だけを行う `detect_default_ffmpeg_path()` を新設し、「既定」ボタンからはそちらを呼ぶよう修正 |
| 4 | フォルダをドラッグ&ドロップすると、そのままファイル一覧に加わり、変換時に原因不明の失敗ログだけが出る | ドロップされたパスがファイルかどうかを確認していなかった | フォルダがドロップされた場合は一覧に加えず、理由をログに表示するよう修正 |
| 5 | ffmpeg パスの保存(設定ファイルへの書き込み)に失敗しても何も表示されず、「設定したのに再起動すると消える」ように見える(例: 書き込み権限のないフォルダに配置した場合) | 保存関数のエラーを `let _ = ...` で握りつぶしていた | 保存失敗時はログにその旨と理由を表示するよう、該当する全箇所を修正 |
| 6 | (軽微) `cargo clippy` で `src/logic.rs` のテストコードに `neg_cmp_op_on_partial_ord` の警告 | `!(d > 120.0)` という否定比較を使っていた | `d <= 120.0` に書き換え |

上記に加え、「動作確認」「参照…」ボタンにホバーテキストを追加し、
何をするボタンかその場で分かるようにした。

## 今回追加で行った検証

- `cargo build --release` … Linux ホストでフルビルドが成功することを確認
  (Windows 専用の `#[cfg(windows)]` 分岐以外の全コードパスがコンパイル
  可能であることの強い裏付けになる)
- `cargo test --release` … 11件全て合格
- `cargo clippy --release --all-targets` … 警告 0件(#6 の1件を修正後)
- `cargo fmt --check` … 差分なし
- ソース・ドキュメント・生成済み PDF マニュアルに個人情報(実際のユーザー名・
  メールアドレス・実機のファイルパス等)が含まれていないことを再確認
  (`grep` によるパターン検索。マニュアル中の `C:\Users\...\greeting.wav` は
  実在のユーザー名を含まないプレースホルダ例であることを確認済み)
- 生成された `Cargo.lock` をリポジトリに含めることにした
  (バイナリクレートでの再現可能なビルドのため。以前はビルド自体が
  行えず生成されていなかった)

## 商標名(タカコム/ナカヨ/岩崎通信機)の扱いについて

README に、これらの名称が各社の商標であり本プロジェクトが非公式・
無関係であることを明記した「商標について」節を追加した。
名称は「どの機器向けの音声仕様に対応しているか」を説明する事実の記述
としてのみ使用しており、ロゴ等の商標そのものは使用していない。

---

# 修正内容一覧 (v0.1.0 → v0.2.0)

## 見つかった不具合・想定外入力への対応漏れ・ロジックの矛盾と修正

| # | 症状 | 原因 | 修正内容 |
|---|---|---|---|
| 1 | 岩崎通信機プリセットで、録音時間を取得できないファイルが無制限に変換されてしまう | 長さ取得失敗時に警告ログのみ出して変換を継続していた | 上限超過ファイルと同様にスキップし、専用の理由付きで警告ダイアログに表示するよう変更 |
| 2 | 一度設定した ffmpeg のパスが、再起動すると勝手に消える(自動検出に戻ってしまう)ことがある | 保存されたパスが `ffmpeg` のような「PATH解決前提の裸のコマンド名」だった場合、`Path::exists()` チェックがカレントディレクトリ基準で失敗し、無効な値として棄却していた | 「区切り文字を含まない裸のコマンド名」は存在チェックをスキップして信頼するよう修正(`is_bare_command`) |
| 3 | 「動作確認」ボタンを押すと、指定先が応答しない実行ファイルの場合に画面全体が固まる | `check_ffmpeg()` をUIスレッドで同期実行していた | バックグラウンドスレッドで実行し、結果をログに非同期反映するよう変更 |
| 4 | 同名ファイル(同じファイル名を持つ入力を2つ選ぶ、または別フォルダの同名ファイルを共通の出力先に変換する等)を続けて変換すると、後から変換した方が前のファイルを上書きしてしまう | 出力ファイル名が「元ファイル名+秒単位のタイムスタンプ」のみで一意性を保証していなかった | 出力先に同名ファイルが既にある場合は自動的に `(2)`, `(3)` ... を付けて重複を回避するよう修正 |
| 5 | 大文字小文字だけが異なる同一ファイル(例: `A.wav` と `a.WAV`)を選ぶと、Windows 上は同じファイルなのに一覧に重複登録されてしまう | ファイルの重複チェックが `PathBuf` の完全一致のみで、Windows のファイルシステムが大文字小文字を区別しないことを考慮していなかった | Windows では大文字小文字を無視して比較する `paths_equal()` を追加し、ファイル選択・ドラッグ&ドロップの両方に適用 |
| 6 | `ffmpeg_args()` が `Option<Vec<String>>` を返す設計になっており、実際には全プリセットが具体的な仕様を持つのに「仕様未定→None」という到達不能な分岐が残っていた | 開発初期(岩崎通信機の仕様待ち)の名残 | 戻り値の型を `Vec<String>` に単純化し、死んだ分岐を削除 |
| 7 | ワーカースレッドが1つ異常終了(パニック)すると、以降「変換中」フラグ等の状態が誤った既定値にフォールバックし、多重変換などの不整合を招きうる | `Mutex::lock()` のポイズニングエラーを `if let Ok(...)` で握りつぶし、中身は無事なのに「ロック失敗=false」として扱っていた | ポイズンを無視してロックを復旧する `lock_ignore_poison()` ヘルパーを導入し、全箇所に適用 |
| 8 | ウィンドウ/タスクバーアイコンを読み込むはずの関数が、実際にはファイルを読み込むだけで一切パースせず常に「アイコンなし」を返していた(死んだコード) | 実装が完了しないまま放置されていた | Windows では実行ファイルのリソースに埋め込んだアイコン(`build.rs` 経由)が自動的にタイトルバー・タスクバーに使われるため、この関数自体を削除し、コメントで経緯を明記 |

すべての純粋ロジック(#1, #2, #4, #5 相当)は `src/logic.rs` に分離し、
`cargo test` で実行できる単体テストを追加して動作を確認済みです。

## 依存関係の更新(非推奨・古いクレートの是正)

| クレート | 旧バージョン | 新バージョン | 理由 |
|---|---|---|---|
| `egui` / `eframe` | 0.27 | 0.29 | `0.34`〜`0.36` 系を試したところ、`0.36` 系で `eframe::App` トレイトの形状自体が `update(&mut self, ctx: &egui::Context, frame: &mut eframe::Frame)` から `ui(&mut self, ui: &mut egui::Ui, frame: &mut eframe::Frame)` ベースへ再設計されており(レンダリングバックエンド刷新に伴う変更と見られる)、本プロジェクトのコードと非互換であることが実際のビルドエラーで判明した。この再設計がどのバージョンから入ったかを確実に特定できなかったため、安全のため十分枯れた `0.29` 系(要求 rustc 1.76)に固定した |
| `rfd` | 0.14 | 0.17 | 最新安定版へ更新(使用しているAPIに破壊的変更なし) |
| `winres` | 0.1.12(2021年最終更新、事実上未保守) | `winresource` 0.1系 | `winres` は Rust 1.61 以降で動作しないことがあり保守停止状態。同一APIを維持した保守フォークである `winresource` に切り替え(呼び出しコードは無変更) |

`build.rs` のターゲットOS判定も、クロスコンパイル時に誤動作しうる
`#[cfg(windows)]`(ホストOS依存)から、`CARGO_CFG_TARGET_OS` 環境変数を見る
方式(ターゲットOS依存)に変更しました。

### egui のバージョンについて(重要)

`egui`/`eframe` は非常に頻繁に破壊的変更が入るクレートです。
本プロジェクトでは実際に以下の問題に遭遇しました。

1. **`0.36` 系は rustc 1.95 を要求**し、rustc 1.92 環境ではビルドできない
   (`egui@0.36.1 requires rustc 1.95` のようなエラー)
2. rustc を 1.98.1 に上げて `0.36.1` が実際にビルドされたところ、
   `eframe::App` トレイトの必須メソッドが `update()` から `ui()` に
   変わっており、本プロジェクトのコードは**コンパイルエラー**になった
   (`method update is not a member of trait eframe::App` 等)

このため、rustc バージョンの問題が解決しても egui のバージョンによっては
コードの書き換えが別途必要になる可能性があります。本プロジェクトは
`0.29` 系(2024年9月リリース、要求 rustc 1.76)に固定することでこの
再設計の影響を回避しています。rustc 1.76 以上であれば動作するはずです。

より新しい `egui` に上げたい場合は、1バージョンずつ上げながら
[egui の CHANGELOG](https://github.com/emilk/egui/blob/main/CHANGELOG.md) と
[eframe の CHANGELOG](https://github.com/emilk/egui/blob/main/crates/eframe/CHANGELOG.md)
の "BREAKING" 項目を確認することを強く推奨します。

## 検証方法について

開発環境の制約上、GUI 全体を実際にビルドしての最終確認はサンドボックス内では
できていません(古いRust環境のため)。以下の方法で可能な範囲の検証を行い、
その後お手元の環境で実際に `cargo build --release` していただいた結果を
反映して `egui`/`eframe` のバージョンを調整しました。

- GUI に依存しない純粋ロジック(`src/logic.rs`)は、最小構成のRust環境で
  実際に `cargo test` を実行し、11件のテストすべてが合格することを確認
- `rustfmt` によって全ソースファイルの構文が正しくパースできることを確認
- 依存クレートの破壊的変更は、可能な範囲で各クレートの公式ドキュメント・
  変更履歴を確認した上で反映したが、`egui`/`eframe` の `0.36` 系については
  ドキュメント確認だけでは検出できない破壊的変更(`App` トレイトの再設計)が
  実際のビルドで見つかったため、`0.29` 系に固定し直した経緯がある

**`cargo build --release` の結果、追加のエラーが出た場合はエラーメッセージを
共有してください。** `egui`/`eframe` は特に破壊的変更が多いクレートのため、
バージョンの組み合わせによっては同様の問題が起こり得ます。
